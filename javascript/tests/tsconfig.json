{
  "extends": "../tsconfig.json",
  "references": [
    {
      "path": "../src"
    }
  ],
  "include": [
    // Pathetically, '*' does NOT mean all files, even when JSON support is enabled.
    "*", "*.json"
  ],
  "compilerOptions": {
    "rootDir": "./",
    "outDir": "../dist-tests",
    "composite": true
  }
}